rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // EMULATOR RULES - Komplett offen für lokale Entwicklung
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
